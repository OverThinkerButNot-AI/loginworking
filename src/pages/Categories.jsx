import React from "react";

const Categories = ({ names }) => {
  // console.log(name)
  return (
    <div className="ml-10 ">
     
      <div>{names}</div>
    </div>
  );
};

export default Categories;
